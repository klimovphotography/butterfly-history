# Эффект Бабочки: сайт с альтернативной историей

Готовый сайт: пользователь вводит историческое событие, ИИ выдает сценарий, как могла измениться история.

## Что нужно

- Установленный Node.js (версия 18+)
- Gemini API key (Google AI Studio)

## Быстрый запуск

1. Откройте терминал в папке проекта:
   ```bash
   cd "/Users/vasilijklimov/Documents/Codex project"
   ```
2. Создайте файл `.env` из шаблона:
   ```bash
   cp .env.example .env
   ```
3. В файле `.env` вставьте ваш ключ:
   ```env
   GEMINI_API_KEY=ваш_ключ
   ```
4. Запустите сервер:
   ```bash
   npm start
   ```
5. Откройте в браузере:
   [http://localhost:3000](http://localhost:3000)

## Как пользоваться

1. Введите событие в поле.
2. Или выберите готовый пример в блоке `Быстрый старт`.
3. Нажмите `Смоделировать`.
4. Получите:
   - текстовый сценарий,
   - таймлайн по конкретным годам,
   - 2-3 кнопки `Что делаем дальше?` для продолжения ветки,
   - 1-2 иллюстрации альтернативного мира.
5. Откройте старые сценарии в блоке `История запросов`.

## Файлы проекта

- `server.mjs` - backend и запрос к Gemini API
- `public/index.html` - страница сайта
- `public/style.css` - стили
- `public/app.js` - логика чата в браузере
